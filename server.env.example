# CORS Origins for WebRTC Server
# Comma-separated list of allowed origins for CORS
# For local development, this can be omitted as it defaults to localhost origins
CORS_ORIGIN=http://localhost:5173,https://your-frontend-domain.com

# Port configuration
# PORT=3000
